[package]
name = "lockstep-engine"
version = "0.1.12"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Pipeline orchestration for lockstep (pair -> strip -> normalize -> compare)."

[dependencies]
lockstep-core = { workspace = true }
lockstep-config = { workspace = true }
lockstep-pairing = { workspace = true }
lockstep-strip = { workspace = true }
lockstep-normalize = { workspace = true }
lockstep-compare = { workspace = true }
git2 = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
lockstep-pairing = { path = "../lockstep-pairing", features = ["test-fixtures"] }
tempfile = "3"
